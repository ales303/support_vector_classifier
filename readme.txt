A simple stock data analysis program to train and run predictions for a support vector classifier that uses k fold cross validation and SMOTETomek to address imbalanced data (creates more of the under-represented class in the data).